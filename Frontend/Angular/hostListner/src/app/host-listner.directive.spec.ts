import { HostListnerDirective } from './host-listner.directive';

describe('HostListnerDirective', () => {
  it('should create an instance', () => {
    const directive = new HostListnerDirective();
    expect(directive).toBeTruthy();
  });
});
